/* TASK 1
Температура: градусы Цельсия в градусы Фаренгейт
*/


let tC = Number(prompt())
let tF = 1.8 * tC + 32
console.log(tF)




/* TASK 2
Величина угла: градусы в радианы
Дана величина угла в градусах -- положительное натуральное число. Переведите величину угла в радианы по формуле radians=degrees*2π/360
Sample Input 1: 180 Sample Output 1: 3.1416
*/


let degrees = Number(prompt())
let radians = (degrees * (2*Math.PI/360)).toFixed(4)
console.log(radians)




/* TASK 3
Столбы
Возле дороги стоят столбы. Расстояние между столбами одинаковое. Все столбы одинакового диаметра.
количество столбов (≥ 1), расстояние между столбами (10-30 метров), диаметр столба (10-50 сантиметров)
Вычислите расстояние между первым и последним столбом в ​​сантиметрах. Диаметры первого и последнего столба учитывать не нужно.
Sample Input: 2 20 25 Sample Output: 2000
*/


function roadside(n) {
    if (n < 2) return 0
    return (n-1) * d*100 + (n-2) * w
}
let [n, d, w] = prompt().split(' ').map(Number)
console.log(roadside(n))


/* TASK 4
Простые обои
Иван решил поклеить комнату обоями. Прямоугольная комната имеет длину lметров, ширину wметров и высоту hметров. Стандартная ширина рулонов — 52сантиметра, длина рулона 10метров. 
Рулоны (даже неполной ширины) укладываются встык -- вплотную друг к другую без наложений. Также нужен запасной отрезок обоев на случай ошибки или просчета, 
поэтому он хочет купить на 15% больше, чем нужно. Входные данные: Размер комнаты три вещественных числа l, w, h -- длина, ширина и высота в метрах.
Выходные данные: Число рулонов будет меньше или равно 20. Пример: Для 4.0 3.5 3.0 нужно 10 рулонов.
*/


function wall(l, w, h) {
    let skeins = Math.ceil(((l + w) * 200 / 52) * h / 10 * 1.15)
    if (l * w === 0) return 0
    return skeins
}
let [l, w, h] = prompt().split(' ').map(Number)
console.log(wall(l, w, h))


/* TASK 5
Площадь круга внутри квадрата. Превратите площадь квадрата в площадь круга, который помещается внутри квадрата.
Вы получаете положительное вещественное число, представляющее площадь квадрата, и должны вернуть площадь круга.
Sample Input 1: 9 Sample Output 1: 7.0686
Sample Input 2: 20 Sample Output 2: 15.708
*/


let s = Number(prompt())
let a = Math.sqrt(s)
console.log(((Math.PI * (a**2))/4).toFixed(4)/10*10)
